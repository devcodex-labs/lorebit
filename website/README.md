# lorebit documentation site

This folder is the Rspress build surface. User-facing content lives in the repository-root `../docs/` directory so documentation remains one source of truth.

## Commands

```bash
npm install
npm run dev
npm test
```

GitHub Pages publishes the generated `dist/` directory through `.github/workflows/pages.yml`.
