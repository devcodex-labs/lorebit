# 预览状态与公开契约

<span class="lorebit-label">参考 · 先区分已存在事实与目标设计</span>

## 当前已存在

- 本用户文档站与公开仓库。
- 已冻结的产品边界：通用、RAG-first、local-first、provider-neutral。
- 已冻结的存储边界：数据库与索引通过 adapter 接入，core 不实现数据库内核。

## 当前不存在

- `@devcodex/lorebit` npm 包。
- 可安装的 SDK、稳定 API、CLI 或配置文件。
- 生产级数据库 adapter、托管服务或数据库实现。

## 如何阅读本网站

本站有两种明确区分的内容：**当前事实**描述今天已存在的仓库与文档；**0.x 目标行为合同**描述后续实现必须兑现的用户结果。概念页不会把某个尚未发布函数或字段名写成承诺，目标合同也不会伪装成可下载安装说明。

等公开实现出现后，文档会把每一项能力标注为可用版本、支持条件和失败恢复路径；在此之前，使用者可用目标合同和验收场景评审自己的采用路径。

如果你正在选择技术路线，请把本站作为“应具备什么知识工作流”的依据，而不是立即接入指南。

## 今天可以做什么

| 你的目标 | 可以使用本站完成的事 |
|---|---|
| 判断是否值得采用 | 对照首页的适用性边界，确认问题是否需要来源、引用和更新控制。 |
| 让团队对齐 | 用第一条工作流、资料与证据、生命周期页面共同评审一条真实业务路径。 |
| 准备未来接入 | 明确需要的 adapter 能力、质量检查和失败边界。 |
| 约束后续开发 | 用[目标行为合同](/reference/behavior-contract)和[用户场景验收](/reference/acceptance-scenarios)对齐实现范围。 |
| 立即集成运行时 | 暂不支持；等待可验证的公开 SDK、安装方式和恢复说明。 |

下一步：[查看 0.x 目标行为合同](/reference/behavior-contract)。
